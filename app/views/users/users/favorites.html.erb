<% @favorite_posts.each do |favorite| %>
<!--<#%= link_to image_tag( post.image.variant(resize: '100x100') ), post_path(post.id) %>-->
<%= link_to favorite.user.name, user_path(favorite.user.id) %>
<!--<i class="fa fa-heart unlike-btn"></i>-->
<!--<#%= post.likes.length %>-->
<%= link_to favorite.title, post_path(favorite.id) %>
<% end %>